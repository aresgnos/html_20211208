20211216

vue 파일 만들면
vbass-css

패키지.json이 있는 폴더에 들어가서
cmd => npm run serve

소문자, 대문자

데이터베이스 -> 메소드 -> 상태변수 바뀐다 -> 화면 갱신

<tr v-for="(tmp, idx) in items" v-bind:key="tmp">
                 <td>{{idx+1}}</td>
(tmp, idx) = 숫자 넣기(숫자 돌리기)

v-for = "tmp ~ " => 반복문

상태변수만 바꾸면 화면을 갱신할 수 있기 때문에
새로고침x, 화면이 깜빡거리지 않는다.

메소드는 호출되어지는 기능
생명주기에 의해 메소드 호출

tmp : 임시로 값을 저장하기 위한 변수

cmd 작업 끝내고
npm i axios --save

벡앤드에 데이터를 받는 방법 : fetch, axios

메소드 안에 await(기다린다)가 하나라도 들어있으면
메소드 앞에 async를 붙인다.

<!-- --> =주석


주소에서 ?뒤에 있는 것들로 데이터가 구성된다. (query)


링크 눌러서 데이터를 보낼 때
{
    path:'/보낼 페이지', 
    query:{no:2, key:2, name:"},
}                        

=>이 모양으로

여러개를 보낼 때는 {key:  ,name:  ,  }



v가 뭘까

고정할 수 없으면 상태변수로 기본고정값을 만들어라.

배열로 만들면 "code[tmp]"로

상태변수에서 하나로 묶어서 하나만 감시한다. (던진다.)

** 데이터베이스의 가변이 화면과 연동되어야 한다. **
상태변수의 값을 어떻게 바꿀 것이냐!
=결국은 상태변수를 바꾸는 것.
데이터베이스가 왔을 때 상태변수를 어떻게 잘 바꿀 것이냐
(유연하게)


[{}.{},{},{}...]

http://ihongss.com/pdf/exam0.pdf